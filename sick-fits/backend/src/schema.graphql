# Anything we want visible in the API goes here

type Dog {
    name: String!
}

type User {
    id: ID!,
    name: String!
}

type Mutation {
    createDog(name: String!): Dog
}

type Query {
    dogs: [Dog]!
}